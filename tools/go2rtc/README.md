# go2rtc binary

Place the official Windows x64 binary here as `go2rtc.exe`.

Use `scripts\setup.ps1 -DownloadGo2rtc` to download `go2rtc_win64.zip`
directly from the official `AlexxIT/go2rtc` GitHub release API.

